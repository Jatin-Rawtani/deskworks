# Runtime dependencies (mirror of pyproject.toml [project.dependencies]).
# For development install, prefer:  pip install -e .
numpy>=1.24
sentence-transformers>=2.6
rank-bm25>=0.2.2
flask>=3.0
pypdf>=4.0
tomli>=2.0; python_version < "3.11"

# optional, nicer PDF extraction (pdftotext/poppler is auto-used if on PATH):
# pdfplumber>=0.11
